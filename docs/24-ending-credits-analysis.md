# 24 · 실제 엔딩 크레딧·베스트타임 분석

> 2026-07-23 정적 역공학 결과. 이 문서의 “엔딩”은
> `out/wgp2_kr_017.png`~`023.png`에 찍힌 최종 스크롤 크레딧과
> 베스트타임 화면을 뜻한다. `build_ending_logo.py`가 다루는
> `VICTORYS` 에피소드 인터미션과 부팅 직후 저작권 크레딧은 별개다.

## 결론

- 실제 엔딩 문자는 그림 한 장이나 SJIS 문자열이 아니라
  **8×8 2bpp 소형 글꼴의 직접 타일맵 명령열**이다.
- 크레딧의 39개 고정 문구와 이름, 베스트타임 제목·코스명·`びょう`는
  현재 행 폭 안에서 한글로 바꿨다. 완역 45행 중 폭을 넘거나 축약한 행은 0개다.
- 구현 번역에 필요한 고유 한글 음절은 86개이고, 프로젝트의 8pt 글꼴이
  86개를 모두 보유한다. 엔딩 전용 256타일 자원을 만들면 글리프 슬롯도 충분하다.
- 베스트타임 네 줄의 숫자는 저장 데이터에서 런타임에 덮어쓴다.
  숫자 코드는 보존하고 제목·코스명·단위만 번역하면 기록 계산에는 영향이 없다.
- 짧은 한 줄짜리 현지화 표기는 기존 행의 빈 상단 타일을 이용할 수 있다.
  여러 줄짜리 “번역자의 말”은 명령열을 같은 C7 뱅크의 현재 자유 영역으로
  재배치·연장하면 기술적으로 가능하다. 완역 구현 뒤에도 최대 추가 원시 공간은
  679바이트이며, 최대 폭 31타일 기준 10개 논리행이다.

## 실제 데이터와 소비 코드

| 항목 | 주소 | 확인 내용 |
|---|---:|---|
| 엔딩 행 명령열 | `$C7:A2A5~B182` | 3,806바이트, 종료어 `$FFFF` 포함 |
| 초기 포인터 | `$C3:8275` | `LDA #$A2A5` → 행 명령열 시작 주소 저장 |
| 행 인터프리터 | `$C3:8285` | 행 길이만큼 포인터 전진, 첫 타일값 0~5로 처리기 분기 |
| 일반 행 처리 | `$C3:83BC` | C7의 타일맵 단어를 현재 스크롤 행의 VRAM으로 전송 |
| 베스트타임 처리 | `$C3:8409/8417/8425/8433` | 코스별 저장 기록 선택 후 숫자 덮어쓰기 |
| 숫자 변환 | `$C3:8441` | 초·백분의 초를 10으로 나누어 네 숫자 타일 생성 |
| 엔딩 글꼴 로더 | 호출 `$C3:6CFF`, 포인터 `$C3:6D03` | 원본 `$D9:0000` 해제 후 첫 0x1000바이트(256타일) DMA |
| 엔딩 오브젝트 기술자 | `$C3:E343` | 초기화 주소 `$8275`를 참조하여 위 두 경로가 같은 화면임을 확인 |

명령열은 154개 행 레코드와 종료어로 구성된다.

- 실제 문자가 있는 일반 상단/본문 쌍: 41쌍
- 베스트타임 상단/본문 쌍: 4쌍
- 완전 공백인 길이 0 상단/본문 쌍: 31쌍
- 종료 직전 폭 5의 공백 상단/본문 쌍: 1쌍
- 합계: 77쌍

일본어 탁점·반탁점은 본문 타일 위의 별도 상단 행에 `$94/$95`로 놓인다.
한글은 이 오버레이가 필요 없으므로 본문 행만으로 표시할 수 있다.

레코드의 첫 타일값은 화면 문자이면서 처리기 번호다.
일반 행은 0, 베스트타임 네 줄은 각각 1~4다. 원문에서는 이 네 타일이
`あ/い/う/え`로도 보인다. 구현본은 코드 1~4를 보존하고 엔딩 전용 글꼴의
해당 네 타일만 `가/나/다/라`로 바꿨다. 따라서 분기는 원형 그대로다.

## 크레딧 행 폭

역산한 39개 크레딧 행은 역할·회사 13행과 사람 이름 26회
(고유 인물 25명, `めぐろ のりちか` 2회)다.
기존 본문 시작 열을 유지한 후보 번역은 모두 고정 폭에 들어간다.

폭이 정확히 차거나 비교적 타이트한 행은 다음과 같다.

| 원문 | 사용 가능 타일 | 후보 번역 | 필요 타일 |
|---|---:|---|---:|
| `めぐろ のりちか` | 8 | `메구로 노리치카` | 8 |
| `すずき としあき` | 8 | `스즈키 토시아키` | 8 |
| `たわら まさき` | 7 | `타와라 마사키` | 7 |
| `きたわき ひらく` | 8 | `키타와키 히라쿠` | 8 |
| `ふなこし まさのり` | 9 | `후나코시 마사노리` | 9 |
| `もり よしかず` | 7 | `모리 요시카즈` | 7 |
| `たなか あぐる` | 7 | `타나카 아구루` | 7 |
| `しぶや のりこ` | 7 | `시부야 노리코` | 7 |
| `ふじた まさき` | 7 | `후지타 마사키` | 7 |
| `たじま まさる` | 7 | `타지마 마사루` | 7 |
| `おがた はたお` | 7 | `오가타 하타오` | 7 |
| `さがみ やすゆき` | 8 | `사가미 야스유키` | 8 |
| `つちや ひろつぐ` | 8 | `츠치야 히로츠구` | 8 |
| `くしま かずひと` | 8 | `쿠시마 카즈히토` | 8 |
| `まきの もりくに` | 8 | `마키노 모리쿠니` | 8 |
| `なかやま まこと` | 8 | `나카야마 마코토` | 8 |
| `くぼ まさかず` | 7 | `쿠보 마사카즈` | 7 |
| `やまうち ひろし` | 8 | `야마우치 히로시` | 8 |

나머지는 1~7타일의 여유가 있다. 대표 역할명도 다음처럼 들어간다.

| 원문 | 사용 가능 타일 | 후보 번역 | 필요 타일 |
|---|---:|---|---:|
| `せいさく` | 4 | `제작` | 2 |
| `プログラマー` | 6 | `프로그래머` | 5 |
| `せいさくしんこう` | 8 | `제작 진행` | 5 |
| `せいさくきょうりょく` | 10 | `제작 협력` | 5 |
| `スペシャルサンクス` | 9 | `스페셜 땡스` | 6 |
| `エグゼクティブプロデューサー` | 14 | `총괄 프로듀서` | 7 |

고유명사의 실제 표기는 구현 전에 한 번 더 교정할 수 있다.
위 표는 타일 수용량을 검증하기 위한 읽기 기반 후보이며, 표기가 바뀌어도
각 행의 “사용 가능 타일”을 넘지만 않으면 구조 변경은 필요 없다.

## 베스트타임 행

베스트타임 제목은 6타일 슬롯이다.

- `ベストタイム` 6타일 → `베스트타임` 5타일

각 코스 행의 고정 배치는 다음과 같다.

| 열 | 용도 |
|---:|---|
| 0 | 처리기 선택용 1~4. 보존 필요 |
| 1~5 | 공백 |
| 6~17 | 코스명 12타일 |
| 18~19 | 런타임 초 두 자리 |
| 20~22 | 고정 단위 `びょう` |
| 23~24 | 런타임 백분의 초 두 자리 |

| 원문 | 코스명 슬롯 | 후보 번역 | 필요 타일 |
|---|---:|---|---:|
| `たかばやま` | 12 | `타카바산` | 4 |
| `ふうりんサーキット` | 12 | `풍륜 서킷` | 5 |
| `しょうがっこう` | 12 | `초등학교` | 4 |
| `つちやレースじょう` | 12 | `츠치야 경기장` | 7 |
| `びょう` | 3 | `초` | 1 |

숫자는 `$C3:8441`이 타일 `$A0~A9`로 매번 덮어쓴다. 따라서 `$A0~A9`를
한글 글리프로 재사용하지 않고 코스명·단위만 패치해야 한다.

## 글꼴 타일 예산

원본 `$D9:0000`의 해제 크기는 5,984바이트(2bpp 374타일)지만,
엔딩 로더가 올리는 범위는 첫 4,096바이트, 즉 타일 0~255다.
공용 원본을 직접 고치지 않고 엔딩 전용 사본을 만들고
`$C3:6D03`의 소스 포인터만 바꾸는 방식이 안전하다.

보수적으로 다음 슬롯을 모두 보존해도 된다.

- 명령·공백 타일 `$00~$05`: 6개
- 런타임 숫자 `$A0~$A9`: 10개
- 라틴 대문자 `$70~$89`: 26개

256개 중 42개를 보존하면 214개가 남는다.
구현 크레딧·베스트타임에 필요한 고유 한글은 86음절이며
`8pt_font/font-007242d37349daf3_glyph_map.json`에서 86개 모두 확인됐다.
구현은 처리기 `$00~05`와 라틴·원본 숫자·탁점·런타임 숫자가 모인
`$70~A9`를 보수적으로 모두 보존해 192개 후보 타일만 사용한다.
따라서 완역 뒤에도 후보 타일 106개가 남는다.

엔딩 전용 글꼴은 `$D9:E000`에 두었고, 해제 크기 5,984바이트,
LZSS 자원 크기 3,519바이트다. `$C3:6D03`의 인라인 포인터만
`$D9:0000`에서 `$D9:E000`으로 바꿔 공용 소형 글꼴은 수정하지 않았다.

## 번역자 메시지 삽입

### 1. 위치·시간을 바꾸지 않는 짧은 표기

일본어 탁점용 상단 행은 한글화 뒤 비게 된다. 마지막 `END` 쌍의 상단
레코드는 31타일 폭이므로, 첫 명령 타일을 보존하면 최대 30타일짜리
한 줄 표기를 넣고 아래 본문의 `END` 또는 `끝`을 유지할 수 있다.

- 장점: 스트림 포인터·총 길이·종료 타이밍을 바꾸지 않는다.
- 주의: 상단과 본문이 8픽셀 간격이라 문구와 `END`가 가까워질 수 있다.
  실기에서 세로 간격을 확인해야 한다.

### 2. 여러 줄짜리 독립 섹션

현재 최종 ROM에서 `$C7:EE7B~FFFF`는 4,485바이트의 연속 자유 영역이다.
원본 명령열 3,806바이트를 이곳으로 옮기면 679바이트를 추가할 수 있다.

- 시작 포인터 `$C3:8275`의 `$A2A5`를 새 C7 주소로 바꾼다.
- 인터프리터는 별도 길이 상수 없이 `$FFFF`까지 읽는다.
- 종료 플래그도 `$FFFF`를 만났을 때만 세워지므로, 레코드를 추가하면
  스크롤 시간도 자연스럽게 늘어난다.
- 한글 16픽셀 논리행을 “빈 상단 레코드 + 최대 31타일 본문 레코드”로
  만들면 최대 66바이트다. 현재 여유는 최대 폭 기준 10행(660바이트)이다.
  실제 문장이 짧으면 행 사이 공백도 넣을 수 있다.

명령열 내부에는 길이 0의 공백 논리행도 31개 남아 있다. 다만 이 행들은
현재 크레딧 섹션 간격과 스크롤 타이밍을 구성하며, 글자를 넣으면 스트림이
늘어난다. 위치보존 구현 뒤 실제로 쓸 수 있는 확정 예산은 다음과 같이 구분한다.

- 기존 간격을 유지하면서 새로 덧붙일 수 있는 최대 폭 독립행: **10행**
- 기존 간격·타이밍까지 줄여 재사용할 수 있는 길이 0 공백행: **31행**
- 남은 글꼴 후보: **106타일**

## 구현 산출물

- 번역 원장: `assets/translations/ending_credits.json`
  - 원문, 완역, 실제 삽입문을 분리 보관
  - 45행 전부 `abbreviated: false`
- 빌더: `scripts/build_ending_credits.py`
  - 154레코드와 3,806바이트 스트림 길이 보존
  - 베스트타임 처리기와 런타임 숫자 필드 보존
- 회귀: `scripts/test_ending_credits.py`
  - 행 주소·길이·속성, 전용 폰트, 가/나/다/라 처리기,
    추가 10행 예산을 최종 ROM에서 역검증
- 빌드 기록: `out/ending_credits_glyph_map.json`
- 시각 프리뷰: `out/ending_credits_preview.png`

## 구현 시 회귀 게이트

1. 원본 엔딩 명령열 154레코드를 전량 파싱하고 종료어 위치를 검증한다.
2. 일반 행의 첫 타일 0과 베스트타임 행의 1~4를 보존한다.
3. 런타임 숫자 타일 `$A0~A9`와 숫자 기록 주소를 보존한다.
4. 전용 글꼴은 원본 `$D9:0000`을 수정하지 않고 별도 자원으로 만든다.
5. 스트림을 재배치하면 C7 뱅크 안에 두고 `$FFFF` 종료까지 다시 파싱한다.
6. `scripts/test_roundtrip.py`, 전체 빌드, 2MiB 크기, BPS 적용 동일성 검사를 통과한다.
7. Mesen2에서 크레딧 전체 스크롤, 네 코스의 서로 다른 저장 기록,
   추가 메시지의 진입·종료 타이밍을 실기로 확인한다.
