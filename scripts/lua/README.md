# Mesen2 Lua 도구

| 경로 | 용도 |
|---|---|
| `capture/` | WRAM·VRAM·CGRAM 덤프와 자동 스크린샷 |
| `probes/` | API 동작이나 특정 화면 상태를 짧게 확인하는 조사 |
| `traces/` | 실행 PC, DMA, 글꼴·텍스트 소스 주소를 지속 추적 |

스크립트는 ROM에 패치를 쓰는 빌드 도구가 아니라 실기 검증·역공학 보조 도구다. 새 Lua 파일은
주된 목적 하나를 골라 해당 디렉터리에 두고 문서에는 저장소 루트 기준 경로를 기록한다.
